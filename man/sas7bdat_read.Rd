% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reader.R
\name{sas7bdat_read}
\alias{sas7bdat_read}
\alias{sas7bdat_open}
\alias{sas7bdat_read_chunk}
\alias{sas7bdat_close}
\alias{sas7bdat_info}
\title{Read SAS files in continuous chunks}
\usage{
sas7bdat_read(
  path,
  callback,
  chunk_rows = 100000L,
  chunk_bytes = "128 MiB",
  columns = NULL,
  encoding = NULL,
  dates = TRUE
)

sas7bdat_open(
  path,
  chunk_rows = 100000L,
  chunk_bytes = "128 MiB",
  columns = NULL,
  encoding = NULL,
  dates = TRUE
)

sas7bdat_read_chunk(reader, n = NULL)

sas7bdat_close(reader)

sas7bdat_info(reader)
}
\arguments{
\item{path}{A character string specifying the path to a local \code{.sas7bdat} file.}

\item{callback}{A function whose first argument receives each chunk as a data frame.}

\item{chunk_rows}{A positive whole number specifying the maximum rows per chunk.
Fewer rows are returned when the \code{chunk_bytes} target is reached first or
when fewer rows remain in the file.}

\item{chunk_bytes}{Approximate decoded size target for each chunk, as a
numeric byte count or a size string with units B, KB, MB, GB, KiB, MiB,
or GiB (case-insensitive).}

\item{columns}{A character vector of column names, or \code{NULL} for all columns.}

\item{encoding}{A character string specifying the source encoding, or \code{NULL}
to use the encoding recorded in the file.}

\item{dates}{A logical value indicating whether to convert recognized SAS
date/time formats to R date/time classes.}

\item{reader}{A \code{sas7bdat_reader} object created by \code{sas7bdat_open()}.}

\item{n}{A positive whole number specifying the maximum rows for this read,
or \code{NULL} to use the reader's \code{chunk_rows}. The reader's \code{chunk_bytes}
target still applies.}
}
\value{
\itemize{
\item \code{sas7bdat_read()} returns an invisible reading summary: \code{rows} is the
number of rows passed to the callback, \code{chunks} is the number of calls,
and \code{completed} indicates whether reading reached the end of the file.
An empty file calls the callback zero times and returns a completed
summary with zero rows.
\item \code{sas7bdat_open()} returns a \code{sas7bdat_reader} that remembers the reading
position in the open file.
\item \code{sas7bdat_read_chunk()} returns a data frame, or \code{NULL} when no rows remain.
\item \code{sas7bdat_info()} returns a snapshot list with \code{rows_total}, \code{rows_delivered},
\code{columns} (names, types, formats, and labels), \code{compression}, and \code{status}
(\code{open}, \code{eof}, \code{error}, or \code{closed}). \code{eof} means end of file. Additional
diagnostic counters are \code{rows_decoded}, \code{file_opens}, \code{file_closes},
\code{bytes_read}, and \code{seeks}. Byte and seek counters include metadata setup;
they do not measure R memory use.
\item \code{sas7bdat_close()} returns \code{NULL}, invisibly.
}
}
\description{
Read a SAS file a group of rows at a time. Use \code{sas7bdat_read()} to run a
function on every chunk automatically, or use \code{sas7bdat_open()} and
\code{sas7bdat_read_chunk()} to request data frames yourself. Inspect the reader
with \code{sas7bdat_info()} and close it with \code{sas7bdat_close()} when finished.
}
\details{
With \code{sas7bdat_read()}, each chunk is passed to \code{callback} as its
first positional argument.
That argument contains the actual data frame for the current chunk. You
choose its name; it does not have to be named \code{chunk}. The package calls
your function again with the next data frame until reading finishes.

\code{sas7bdat_read()} returns a reading summary, not the dataset or your
callback's results. Assignments inside the callback are local to that
call. To use rows or calculated results after reading, your callback must
save them in an object outside the callback or write them to a file.
Callback return values are not collected; returning exactly \code{FALSE}
stops reading.

With \code{sas7bdat_open()}, the returned reader is a bookmark attached to the
open file, not a data frame. Each \code{sas7bdat_read_chunk(reader)} returns
the next data frame and advances the position. Assigning it to a variable
replaces that variable's previous value; it does not append rows. In a
loop, stop when the result is \code{NULL}. Previously returned data frames stay
valid after later reads or closing. \code{sas7bdat_info()} inspects the reader
without advancing it.

Information returned by \code{sas7bdat_info()} is a snapshot at the time of the
call. A saved list does not update when rows are read or the reader is
closed. Call \code{sas7bdat_info()} again to get the current status and counters.

Reopening the file starts from the beginning. Readers cannot be saved with
\code{saveRDS()} for later reuse or shared between R processes.

The file is opened once and row reading continues across chunk
boundaries without restarting the parser. Metadata setup may seek within
the file once at initialization. Internal SAS RLE and RDC compression are
detected automatically. The file must be seekable; ZIP/gzip wrappers are
unsupported.

Column names are case-sensitive and must be unique. Output columns follow
the requested order. Returned text is converted to UTF-8.

\code{chunk_rows} sets an upper limit on rows per chunk. Each chunk ends after
a complete row when either the row limit or the approximate decoded size
target, \code{chunk_bytes}, is reached. If the byte target is reached first,
the chunk contains fewer than \code{chunk_rows} rows, even before the end of
the file. Row counts can vary between chunks as decoded row sizes vary.
For \code{sas7bdat_read_chunk()}, supplying \code{n} overrides the row limit for that
call; the byte target remains in effect. The final chunk may also contain
fewer rows if fewer remain in the file.

The byte target is approximate and may be exceeded by the last row added
to a chunk. It is not a limit on total R memory usage; even a single row
may exceed the target.
MB and GB use powers of 1,000; MiB and GiB use powers of 1,024.

SAS numerics are doubles. Special missing values use haven-compatible tagged
NAs; character padding is trimmed by ReadStat. Column labels and SAS formats
are retained as \code{label} and \code{format.sas} attributes. User-defined formats
and SAS catalog files are not interpreted. Files exceeding 2,147,483,647
physical observations are rejected in this version, before counter overflow.

To retain the entire dataset, you must explicitly save the chunks and
combine them, as shown below. The entire decoded dataset
and temporary copies must then fit in memory.
For faster binding of many chunks, use \code{data.table::rbindlist()} from the
optional data.table package in place of \code{do.call(rbind, chunks)}.
It returns a \code{data.table}, which also inherits from \code{data.frame}.
}
\section{Closing and stopping}{

\code{sas7bdat_read()} closes its reader automatically at the end, on an error or
interruption, or when the callback returns exactly \code{FALSE}.

When using \code{sas7bdat_open()}, call \code{sas7bdat_close()} when finished. Inside
a function, use \code{on.exit(sas7bdat_close(reader), add = TRUE)} for cleanup even
if processing fails. Closing more than once is safe. Reading from a closed
reader is an error; open a new reader to start again.
}

\section{Example files}{

The package includes three files generated with SAS 9.4:
\itemize{
\item \code{example_uncompressed.sas7bdat}: uncompressed.
\item \code{example_rle.sas7bdat}: RLE compression (\code{COMPRESS=CHAR}).
\item \code{example_rdc.sas7bdat}: RDC compression (\code{COMPRESS=BINARY}).
}

Each contains the same 120 rows with numeric values, Unicode text, dates,
times, datetimes, column labels, and ordinary and special missing values.
Use any of these filenames in \code{system.file()} in the examples below.
The SAS program used to generate them is included as \code{examples/generate.sas}.
SAS is not needed to read the bundled files.
}

\examples{
# Read the entire file into sas_data using a callback.
path <- system.file("examples", "example_uncompressed.sas7bdat",
                    package = "anotherSAS7bdat")
chunks <- list()

sas7bdat_read(
  path,
  chunk_rows = 25,
  callback = function(data) {
    chunks[[length(chunks) + 1L]] <<- data
  }
)

sas_data <- do.call(rbind, chunks)
# Faster alternative with data.table installed:
# sas_data <- data.table::rbindlist(chunks, use.names = TRUE)
rm(chunks)

# Read the entire RLE-compressed file using a reader.
path <- system.file("examples", "example_rle.sas7bdat",
                    package = "anotherSAS7bdat")
reader <- sas7bdat_open(path, chunk_rows = 25)
chunks <- list()

repeat {
  data <- sas7bdat_read_chunk(reader)
  if (is.null(data)) break
  chunks[[length(chunks) + 1L]] <- data
}

sas7bdat_close(reader)

sas_data <- do.call(rbind, chunks)
# Faster alternative with data.table installed:
# sas_data <- data.table::rbindlist(chunks, use.names = TRUE)
rm(chunks)
sas7bdat_info(reader)$status  # "closed"
}
